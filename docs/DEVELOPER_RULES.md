# Developer Rules

- Work inside the existing `Main` folder.
- Keep MVC + Service + Repository architecture.
- Do not duplicate routes, models, or services.
- Validate request payloads with Joi.
- Return consistent JSON responses.
- Never store raw passwords or raw tokens.
- Keep frontend API calls aligned with backend routes.
- Add documentation when behavior changes.
