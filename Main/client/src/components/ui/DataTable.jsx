import React from 'react';

export const DataTable = ({ columns, rows, emptyMessage = 'No records available.' }) => <div className="overflow-x-auto rounded-lg border border-trust-ink/15 bg-white"><table className="w-full min-w-[560px] text-left text-sm"><thead className="border-b border-trust-ink/15 bg-trust-paper"><tr>{columns.map((column) => <th className="px-4 py-3 font-semibold" key={column.key}>{column.label}</th>)}</tr></thead><tbody>{rows.length ? rows.map((row, index) => <tr className="border-b border-trust-ink/10 last:border-0" key={row.id || index}>{columns.map((column) => <td className="px-4 py-3" key={column.key}>{column.render ? column.render(row) : row[column.key]}</td>)}</tr>) : <tr><td className="px-4 py-8 text-center text-trust-slate" colSpan={columns.length}>{emptyMessage}</td></tr>}</tbody></table></div>;
